{
  "name": "encryption-messaging",
  "version": "1.0.0",
  "description": "For each message: 1. Generate a random symmetric key (AES-256) 2. Encrypt message with AES-256 (fast) 3. Encrypt the AES key with recipient's RSA public key (slow but key is tiny) 4. Send both encrypted message + encrypted key",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com-personal:chayan-mann/encryption-messaging.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "crypto": "^1.0.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "ws": "^8.21.3"
  }
}
